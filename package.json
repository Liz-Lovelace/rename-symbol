{
  "name": "rename-symbol",
  "scripts": {
    "start": "yarn run set_up_test_dir && node src/main.js",
    "set_up_test_dir": "rm -rf testdir && cp -r testdir_template testdir",
    "dev": "nodemon -e js --watch src --exec \"yarn run start\"",
    "lint": "eslint src/**/*.js"
  },
  "version": "1.0.0",
  "type": "module",
  "description": "Fuzzy find and replace a symbol across a project!",
  "main": "index.js",
  "author": "Liz",
  "license": "CC0-1.0",
  "devDependencies": {
    "eslint": "^8.38.0",
    "nodemon": "^2.0.22"
  },
  "dependencies": {
    "blessed": "^0.1.81"
  }
}
